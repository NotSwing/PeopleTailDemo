<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.peopletailwireframes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ReviewPage"></activity>
        <activity android:name=".ReviewsPage" />
        <activity android:name=".PeopleTailMainView" />
        <activity android:name=".ResetSuccess" />
        <activity android:name=".NewPassword" />
        <activity android:name=".RestPassword" />
        <activity android:name=".SignInFail" />
        <activity android:name=".SignIn" />
        <activity android:name=".SignUpMain" />
        <activity android:name=".SignUpSignIn" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>